# Dependencies (this project is dependency-free, but just in case)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Secrets — NEVER commit real API keys. Keys live server-side only.
.env
.env.*
!.env.example
server/.env

# Build / temp
dist/
build/
.cache/
.tmp/
